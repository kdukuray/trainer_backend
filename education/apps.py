from django.apps import AppConfig


class EducationConfig(AppConfig):
    """Quiz questions, flashcards, and knowledge ranking."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "education"
